<nav class="site-nav">
  <div class="container nav-inner">
    <a class="nav-brand" href="/" aria-label="Kickback home">
      <img src="/assets/wordmark-on-light.svg" alt="kickback">
    </a>
    <div class="nav-links">
      {% for item in site.nav %}
      <a href="{{ item.href }}"{% if activeNav == item.key %} aria-current="page" class="nav-link-active"{% endif %}>{{ item.label }}</a>
      {% endfor %}
    </div>
    <div class="nav-actions">
      <a class="nav-signin" href="{{ site.signInUrl }}">Sign In</a>
      <a class="ks-btn ks-btn-primary ks-btn-sm" href="{{ site.platformUrl }}">Get Started</a>
    </div>
  </div>
</nav>
